import {BaseRepository} from "../../../../repository/baseRepository";
import {StateMachineModel} from "../models/stateMachine";

export class StateMachineRepository extends BaseRepository {
    model = StateMachineModel;
}
